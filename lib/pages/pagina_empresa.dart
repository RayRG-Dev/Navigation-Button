import 'package:flutter/material.dart';

class PaginaEmpresa extends StatelessWidget {
  const PaginaEmpresa({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Pagina de la empresa'),
    );
  }
}
